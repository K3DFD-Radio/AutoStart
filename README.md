# AutoStart - 
## A Python script that sequentially starts and stops PC programs using simple .json instructions
### Run the Python script from your favorite editor or use the provided executable. Use a text editor line Notepad++ to edit the autostart.json file to specify which programs to start, the sequence to start them in and the delay in seconds between each app.
